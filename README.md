# My Angular

This is an experimental project that uses the Angular framework to build a web application. This repository contains a collection of reusable UI components built with Angular. The purpose of this project is to explore and learn the basics of Angular and its capabilities.

`yarn install`

`yarn start`

## Storybook

`yarn run storybook`
